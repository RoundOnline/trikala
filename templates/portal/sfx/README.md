# sfx/

Short audio effects. Loaded fully into memory by `kira` so playback
latency is zero.

Drop a `whoosh.ogg` here and trigger it on portal crossing for an
instant "wait that did something" feel. See the blank template's
[sfx/README.md](../../blank/sfx/README.md) for conventions.
