# music/

Background tracks. Long-form audio, streamed by `kira` from disk so
RAM stays clean.

This portal template doesn't play music by default — wire it in
when you add `kira` to your `Cargo.toml`. See the blank template's
[music/README.md](../../blank/music/README.md) for format + naming
conventions.
