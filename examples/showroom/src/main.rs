//! trikala-showroom — vision artifact for alpha.2.
//!
//! When complete, this demo will render landscape + 3D model + font +
//! HUD UI in one window, with section-commented code so humans and AI
//! agents can navigate it in 30 seconds and pick the path that matches
//! the game they want to build.
//!
//! For the full spec see `docs/showroom-plan.md`.
//!
//! Today (alpha.1) this is a stub that compiles and prints a pointer.

fn main() {
    println!("trikala-showroom — coming in alpha.2");
    println!();
    println!("This demo will show landscape + 3D model + font + HUD UI");
    println!("on one screen, so you can see what trikala can produce and");
    println!("decide which direction to take your game.");
    println!();
    println!("See docs/showroom-plan.md for the vision spec.");
    println!();
    println!("To see what runs today: cargo run -p trikala-hello");
}
